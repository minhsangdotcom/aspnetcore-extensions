# 🚀 TranMinhSang.AspNetCore.Extensions

**A lightweight and extensible utility library for ASP.NET Core**, providing helper methods for string manipulation, type conversion, reflection, expressions, encryption, compression, and more — all in one elegant package.


## 📦 Installation

You can install via **NuGet Package Manager**:

```bash
dotnet add package TranMinhSang.AspNetCore.Extensions
```

